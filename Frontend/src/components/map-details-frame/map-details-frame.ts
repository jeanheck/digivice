/**
 * UI width for detail map frames (MapDetailsFrame, Seabed, Mobius).
 * Distinct from MAP_DISPLAY_WIDTH_PX = 512, which is the quest-coordinate canvas size.
 */
export const MAP_FRAME_WIDTH_PX = 600;
export const MAP_FRAME_MAX_HEIGHT_PX = 520;
