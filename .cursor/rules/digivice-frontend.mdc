---
description: Convenções básicas Frontend Vue/TS Digivice — sintaxe, imports, naming, sem testes
globs: Frontend/**/*
alwaysApply: false
---

# Digivice — Frontend (básico)

Regras especializadas por pasta:
- `digivice-frontend-data.mdc` — presenters, services, repositories, viewmodels, extensions, assets.
- `digivice-frontend-components.mdc` — componentes Vue, estilo DW3, tooltips, modais.
- `digivice-frontend-sync.mdc` — events (DTO/converter), stores/syncers, models.

## Convenções básicas

1. Sempre `;` no fim das instruções.
2. Prefira aspas duplas `"` em strings.
3. Importações com `@/` (ex.: `@/models`, `@/presenters/map/map.presenter`) em vez de caminhos relativos longos; sem extensão `.ts` (`.vue` mantém a extensão).
4. Arquivos kebab-case com sufixo de papel (`.converter.ts`, `.dto.ts`, `.viewmodel.ts`, `.presenter.ts`, `.service.ts`, `.repository.ts`, `.syncer.ts`, `.selector.ts`, `.extensions.ts`); classes/tipos PascalCase.
5. **Converters:** classe `{AlgumaCoisa}Converter`; arquivo `{alguma-coisa}.converter.ts`; API pública estática `convert` (helpers privados permitidos). Dois contextos, pastas distintas:
   - `events/converters/` — DTO (SignalR) → Model (Pinia)
   - `presenters/converter/` — Raw → ViewModel (dados estáticos)
6. **Sem testes no Frontend** (Vitest, `*.spec.ts`, `__tests__`): decisão intencional. Não criar, sugerir nem incluir, salvo pedido explícito.
